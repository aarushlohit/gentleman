# Contributing

Thanks for your interest in contributing to Gentleman.

- Follow the commit message style: `type(scope): description` (e.g., `feat(android): add accessibility service`).
- Open issues for bugs and feature requests before submitting PRs.
- Run `flutter analyze` and include tests for new logic where practical.
- Keep changes focused and create one commit per logical change.
